//! Request for [`crate::api::JitterRng::next_unit`].

/// Input to [`crate::api::JitterRng::next_unit`] — no parameters are
/// needed; the struct exists to satisfy the uniform request/response contract.
pub struct NextUnitRequest;
