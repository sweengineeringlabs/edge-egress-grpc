//! Request for [`crate::api::GrpcEgress::health_check`].

/// Input to [`crate::api::GrpcEgress::health_check`] — no parameters are
/// needed; the struct exists to satisfy the uniform request/response contract.
pub struct HealthCheckRequest;
