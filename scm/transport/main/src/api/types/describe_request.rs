//! `DescribeRequest` — request marker for [`crate::api::Processor::describe`].

/// Request marker for [`crate::api::Processor::describe`].
#[derive(Debug, Clone, Copy, Default)]
pub struct DescribeRequest;
