//! Mirrors `api::traits` — every `pub trait` declared in api/traits/ is
//! implemented by a concrete type elsewhere in this module; see
//! `core_implements_api_traits` offenders for the per-trait impl sites.
