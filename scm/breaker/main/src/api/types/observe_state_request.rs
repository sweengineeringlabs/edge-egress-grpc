//! Request for [`crate::api::traits::BreakerObservable::state`].

/// Input to [`crate::api::traits::BreakerObservable::state`] — no
/// parameters are needed; the struct exists to satisfy the uniform
/// request/response contract.
pub struct ObserveStateRequest;
