//! Request for [`crate::api::traits::Processor::describe`].

/// Input to [`crate::api::traits::Processor::describe`] — no parameters are
/// needed; the struct exists to satisfy the uniform request/response contract.
pub struct DescribeRequest;
