//! Request for [`crate::api::ConfigBuilderProvider::create_config_builder`].

/// Input to [`crate::api::ConfigBuilderProvider::create_config_builder`]
/// — no parameters are needed; the struct exists to satisfy the uniform
/// request/response contract.
pub struct ConfigBuilderRequest;
