//! Request for [`crate::api::Processor::describe`].

/// Input to [`crate::api::Processor::describe`] — no parameters are
/// needed; the struct exists to satisfy the uniform request/response contract.
pub struct DescribeRequest;
