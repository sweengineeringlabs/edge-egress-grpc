//! Domain error type alias for `swe_edge_egress_grpc_retry`.

/// Domain error type for `swe_edge_egress_grpc_retry`.
#[expect(
    dead_code,
    reason = "SEA api/ type alias — re-exported name, not directly referenced yet"
)]
pub type RetryDomainError = crate::api::error::Error;
